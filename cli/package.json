{
  "name": "nix-rice",
  "version": "0.1.0",
  "description": "CLI for the rice registry — build / validate / add (stub).",
  "bin": {
    "nix-rice": "src/index.ts"
  },
  "type": "module",
  "scripts": {
    "build": "bun src/index.ts build",
    "validate": "bun src/index.ts validate",
    "add": "bun src/index.ts add"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
